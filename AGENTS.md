# 하네스톤 1회차 — 팀 작업 레포

> 🔒 **이 파일은 공용 파일입니다. 수정하려면 조장 승인이 필요합니다.**

## 우리가 만드는 것

**하네스**입니다. 디자인이 아닙니다.

PRD 문서 하나를 입력하면, 여러 sub agent가 단계별로 일해서
**Penpot(=Figma 호환) 디자인**을 만들어내는 자동화 파이프라인을 만듭니다.

디자인은 하네스를 **실행**해서 나옵니다. 손으로 그리지 않습니다.

## 구조

```
.claude/
├── skills/
│   ├── start/SKILL.md         🔒 공용 — 진입점. 단계 호출 순서를 정의
│   ├── scaffold-harness/      🔒 공용 — 확정된 중간단계 → 하네스 초안 생성 (조장이 실행)
│   ├── git/SKILL.md           🔒 공용 — 작업 저장용
│   └── {내 단계용 skill}/      ✅ 자유 생성
└── agents/
    └── stage-*.md             ← 단계별 sub agent. 담당자만 수정
docs/
├── PRD.md                     ← 입력 PRD (운영이 배포)
├── examples/                  ← 예시 PRD 2종 (당근→증권 / Airbnb→소개팅)
└── artifacts/                 ← 단계 간 중간 산출물이 쌓이는 곳
```

## 하네스 초안 만들기 — 조장이 한 번 실행

설계 세션에서 단계를 확정했다면, `start/SKILL.md`와 sub agent를 손으로 쓰지 않습니다.

```
/scaffold-harness 우리 조 단계 이거야: ①... ②... ③...
```

`start`의 단계 표·실행순서와 `.claude/agents/stage-*.md`가 한 번에 생성됩니다.
**초안입니다.** 여기서부터 각자 자기 단계 agent를 고도화하는 것이 오늘의 본 작업입니다.

⚠️ 팀원이 각자 돌리면 서로의 설계를 덮어씁니다. **조장만** 실행하세요.
(이미 있는 `stage-*.md`는 덮어쓰지 않습니다.)

## 규칙 — 이것만 지키면 충돌이 안 납니다

| 행위 | 허용 |
|---|---|
| 내가 맡은 단계의 agent 파일 고도화 | ✅ 자유 |
| 내 단계용 skill 새로 생성 | ✅ 자유 |
| **남이 맡은 단계 수정** | ❌ **금지** |
| **공용 파일 수정** (`AGENTS.md`, `start/SKILL.md`) | ⚠️ **조장 승인** |
| **새 단계(sub agent) 추가** | ⚠️ **조장 승인** + 팀 전체 sync 권장 |

새 단계가 생기면 단계 간 입출력 계약이 바뀝니다. 그래서 혼자 못 늘립니다.

## 단계 설계 원칙

1. **각 단계 = sub agent 1개** (강제)
2. 각 sub agent는 **이전 단계 산출물 또는 PRD를 읽어** 자기 산출물을 만든다
   (또는 figma/pigma를 호출한다)
3. **직렬일 필요 없음** — 입출력 의존관계상 병렬 가능하면 병렬로

## 작업 저장

```
/git 지금까지 작업내용 저장해줘
```

브랜치는 `main`만 씁니다. 자세한 건 `.claude/skills/git/SKILL.md`.

## Penpot 저작 방법

`penpot` MCP가 `.mcp.json`에 이미 등록되어 있습니다. **저작 절차를 규정하는 스킬은
일부러 넣지 않았습니다.** MCP를 어떻게 쓸지는 각 팀이 알아서 설계하세요 — 그 설계가
곧 하네스이고, 채점 대상입니다.

- `use_figma` 툴로 코드를 실행하고, 코드 안에서 `figma.*` (Figma Plugin API)를 씁니다.
  엔진은 Penpot이지만 인터페이스는 Figma로 통일했습니다. `penpot.*`도 병행 가능합니다.
- 함수 본문처럼 작성하고 `return`으로 결과를 받습니다. `console.log`는 반환되지 않습니다.
- 한 번에 몰아 넣지 말고 **작게 쪼개 실행 + 검증**을 반복하세요.
  미지원 `figma.*` 호출은 명확한 에러로 안내됩니다 → 그때 `penpot.*`로 대체합니다.

### 알려진 함정 (운영이 예시 PRD로 리허설하며 실제로 부딪힌 것들)

저작 절차는 자유지만, 아래는 **모르면 시간을 통째로 날리는** 지점입니다.

| 함정 | 대응 |
|---|---|
| `figma.variables.*` 는 성공 응답만 오고 토큰이 거의 안 남는다 | 토큰은 **JS 상수 객체**로 두고 저작에 일관 적용. 점수는 컴포넌트로 받는다 |
| 인스턴스의 `fills` 오버라이드 금지 (`Value not valid`) | 색이 다른 상태(상승/하락, 선택/비선택)는 **별도 컴포넌트**로 |
| 인스턴스의 `characters`(텍스트) 오버라이드는 **된다** | 데이터가 다른 행은 인스턴스 재사용으로 처리 |
| 만든 컴포넌트의 **이름 변경·자식 remove** → 플러그인이 멈춘다 | 이름·구조는 처음에 확정. 잘못 만들었으면 **새 이름으로 새로** 만든다 |
| `layoutGrow` Spacer가 폭 1로 되돌아간다 | 하단 고정 요소는 Spacer 높이를 **계산해서 명시** |
| hug(자동 폭) 칸은 텍스트를 갈아끼워도 위치가 안 따라온다 | 가변 텍스트 칸은 **고정 폭 + 텍스트 정렬** |
| `primaryAxisSizingMode` 등 figma 사이징 프로퍼티가 안 먹는다 | `node.horizontalSizing = "fix"\|"auto"` (penpot 쪽)을 쓴다 |
| **컴포넌트 이름이 파일 전역** — 옆 팀원 Page의 동명 컴포넌트가 잡힌다 | 이름 + **id 프리픽스**로 좁혀서 찾는다 |
| 기존 파일 폰트가 **서버에 없으면 조용히 대체**된다 (에러 없음) | `penpot.fonts.all` 로 먼저 확인하고 대체 폰트를 정한다 |
| 비-오토레이아웃 프레임은 `appendChild`가 자식을 **안 옮긴다** | 붙인 뒤 `c.x = parent.x + dx; c.y = parent.y + dy` |
| 고정 폭 텍스트를 `growType="fixed"`로 두면 **글자가 잘린다** | `growType = "auto-height"` |
| `export_shape`가 레이아웃 안정 전에 찍히면 **빈 영역**이 나온다 | 없다고 판단하기 전에 **재-export 한 번** |

**쓸 수 있는 것도 알아두세요.**

- **실제 사진을 넣을 수 있습니다.** top-level `await` 도 됩니다.
  `const img = await penpot.uploadMediaUrl(name, url)` → `rect.fills = [{ fillOpacity:1, fillImage: img }]`
- `board.insertChild(index, node)` 로 자식 순서를 고칠 수 있습니다.
  (지우기가 위험한 환경이라 이게 탈출구입니다.)

**저작 → `export_shape`로 PNG 확인 → 수정** 루프를 매 단계 돌리세요.
안 보고 쌓으면 마지막에 전부 어긋나 있습니다.

### 🔴 작업 Page 규칙 (이건 운영 규칙입니다. 반드시 지킵니다)

조 작업 파일에는 **팀원 수 + 2개의 Page**가 있고, 팀원이 실시간으로 공유합니다.
**Page를 잘못 고르면 그 즉시 남의 화면 위에 그립니다.**

| Page | 규칙 |
|---|---|
| `{내 이름}` | 여기서 저작합니다 |
| `기존파일` | **읽기 전용.** 과제의 기존 자산이 여기 있습니다. 수정 금지 |
| `중간공유`·`최종제출` | 공용. 결과를 **옮겨 담는** 곳. 여기서 처음부터 저작하지 않습니다 |

- 저작 전 **작업 Page 이름을 반드시 확정**합니다. 정해지지 않았으면 사용자에게 묻고
  답을 받기 전에는 시작하지 않습니다. 기본값으로 첫 Page를 쓰지 않습니다.
- 하네스를 무인 실행할 때는 Page 이름을 **인자로 받아야** 합니다.
- 읽기만 할 때는 Page를 전환하지 않습니다. **전환하면 남이 보는 화면도 바뀝니다.**

## 채점 축 (최적화 목표) — 2트랙

**A. 디자인 완성도 — 전 참가자 채점, 4항목 × 1~5점 → TOP 3**

| 항목 | 5점 기준 |
|---|---|
| 레이아웃·정렬 | 그리드·여백이 일관되고 정렬이 맞다 |
| 타이포·컬러 | 제목/본문 위계가 명확하고 색이 조화롭다 |
| 완성도·디테일 | 실제 서비스 화면이라 해도 믿을 만하다 |
| PRD 충족도 | PRD가 요구한 화면·요소가 전부 있다 |

**B. 하네스 설계 완성도 — 심사위원(DRI) 채점 → Best 1 (TOP 3 제외 후 선정)**
단계 분할의 타당성 · 단계 간 계약의 명료성 · **재현성(반하드코딩)** ·
**지침의 구체성** · 협업의 흔적.

> 토큰·컴포넌트 재사용·의미기반 네이밍·Frame 위계·Auto Layout은 점수 항목이 아니라
> 위 둘을 동시에 끌어올리는 **수단**입니다.
> **심사용 PRD는 미공개입니다** — 특정 PRD 전용 스크립트는 그 자리에서 무너집니다.
> `docs/examples/`의 PRD 2종 모두에서 돌아가는지 확인하세요.

## 🔴 Penpot — 내 작업 Page를 먼저 확인하세요

중앙 Penpot(`https://sumin-macmini.tail45121d.ts.net`)에 로그인하면
팀 목록에 **`6조`** 가 있습니다. 그 팀의 파일 **`작업`** 을 여세요.

| Page | 담당 |
|---|---|
| **`기존파일`** | **과제의 기존 디자인 파일 — 읽기 대상. 수정 금지** |
| `최원혁` | 최원혁 (조장) |
| `한준희` | 한준희 |
| `하이서` | 하이서 |
| `현수환` | 현수환 |
| `이태경` | 이태경 |
| `중간공유` | 공용 — 결과를 옮겨 담는 곳 |
| `최종제출` | 공용 — **심사 대상** |

**기존 파일은 별도 파일이 아니라 같은 파일의 `기존파일` Page입니다.**
Penpot 플러그인은 **열려 있는 파일 밖을 볼 수 없어서**(`penpot.openFile`이 없습니다)
같은 파일에 있어야 하네스가 읽을 수 있습니다. 읽는 방법은 각 팀이 직접 설계합니다(`penpot.currentFile.pages`).

**실시간 협업이라 엉뚱한 Page에 저작하면 그 즉시 남의 작업 위에 덮습니다.**
그래서 하네스는 **작업 Page가 확정되기 전에는 저작을 시작하지 않고
되묻도록** 만드세요. 하네스를 무인 실행할 때는 Page 이름을 인자로 넘기세요.

`중간공유`·`최종제출`에서 처음부터 저작하지 마세요. 옮겨 담는 곳입니다.

